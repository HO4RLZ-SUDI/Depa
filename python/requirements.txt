# Python dependencies for the Linux/MPU side.
# The `arduino` runtime packages (arduino.app_utils, arduino.app_bricks.*) are
# provided by the App Lab environment and must NOT be listed here.

# --- AI vision: detection + recognition + video (Smart pillar) ---
# Base OpenCV (cv2) is already provided by the Arduino runtime / camera
# peripheral, so we do NOT pin opencv here (avoids a conflicting second build).
# Face DETECTION uses the Haar cascade bundled in base cv2 — no extra deps.
#
# Face RECOGNITION (identity) needs cv2.face from opencv-contrib. If the board
# can install it, uncomment the line below; if not, the app still runs with
# live video + detection and just reports identity as "unknown".
# opencv-contrib-python-headless
numpy                            # array ops (labels, frames)

# --- Notifications / cloud sync (Safe + Data Analytics) ---
requests                  # ntfy / Telegram / webhook push, cloud REST mirror

# --- Voice assistant (Creative pillar) ---
pyttsx3                   # offline text-to-speech (espeak-ng backend)
